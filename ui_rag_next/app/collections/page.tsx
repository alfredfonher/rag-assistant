import { ResourcePage } from "@/components/resource-page";

export default function CollectionsPage() {
  return <ResourcePage resource="Collections" description="Organize related documents into explicit retrieval boundaries." capability="Collection CRUD is represented in the API client. Records will appear only when a backend response schema is integrated." />;
}
